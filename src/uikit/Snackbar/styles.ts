import styled from 'styled-components';
import SnackbarMUI from '@mui/material/Snackbar';
import SnackbarContentMUI from '@mui/material/SnackbarContent';

export const Snackbar = styled(SnackbarMUI)`` as typeof SnackbarMUI;

export const SnackbarContent = styled(SnackbarContentMUI)`` as typeof SnackbarContentMUI;